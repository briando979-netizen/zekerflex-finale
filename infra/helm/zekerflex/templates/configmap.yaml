apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zekerflex.fullname" . }}-config
  labels:
    {{- include "zekerflex.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
