{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "zekerflex.serviceAccountName" . }}
  labels:
    {{- include "zekerflex.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
