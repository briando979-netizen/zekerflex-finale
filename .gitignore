node_modules
.next
out
build
coverage

# Playwright E2E
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

.env
.env.local
.env.*.local

*.log
npm-debug.log*
.DS_Store

.vscode/*
!.vscode/extensions.json

prisma/*.db
next-env.d.ts
*.tsbuildinfo

# local upload storage (sovereign box - files live on disk, not in git)
/storage/
/backups
.env.production

# infrastructure — never commit state or real secrets
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
infra/terraform/terraform.tfvars
infra/terraform/*.auto.tfvars
infra/terraform/backend.hcl
!infra/terraform/env/*.tfvars
infra/k8s/**/secret.yaml
kubeconfig
*.kubeconfig

.vercel

# lokale TTS-cache van scripts/make-uitleg-videos.py
scripts/.tts-cache

# Marketing explainer videos — served from object storage / CDN in production,
# kept on disk for local dev (the /uitleg page degrades gracefully without them).
# ~42 MB; see public/videos/README.md.
public/videos/*.mp4

# Video Studio — gegenereerde projecten (script/manifest wel, media niet)
studio/projects/*/work/
studio/projects/*/clips/
studio/projects/*/voice/
studio/projects/*/out/
.env*
